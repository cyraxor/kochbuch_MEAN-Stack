const receptRouter = require('./recepts')
const ingredientRouter = require('./ingredients')
const categoryRouter = require('./categories')
const preparationRouter = require('./preparation')

module.exports = {
  receptRouter,
  ingredientRouter,
  categoryRouter,
  preparationRouter
}
